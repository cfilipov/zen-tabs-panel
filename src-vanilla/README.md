# Vanilla Source Root

This is the current working extension source, copied from the pre-migration
repo root. `npm run build:vanilla` copies this tree into `dist/`.

Keep this root deployable while the Svelte migration happens in `src-svelte/`.
