name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - run: npm ci

      - name: Build XPI
        run: make package

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: zen-tabs-panel.xpi
          generate_release_notes: false
